import styled from "styled-components";

export const SearchStyledContainer = styled.div`
  margin: 20px auto;
  width: fit-content;

  display: flex;
  align-items: center;
  gap: 10px;

  input {
    padding: 10px;
  }

  button {
    padding: 10px;
    cursor: pointer;
  }
`;
